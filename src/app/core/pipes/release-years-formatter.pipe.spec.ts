import { ReleaseYearsFormatterPipe } from './release-years-formatter.pipe';

describe('ReleaseYearsFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseYearsFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
